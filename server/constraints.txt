# Constraints file to prevent pip from auto-upgrading packages
# This ensures tensorflow 2.15.0 doesn't get upgraded to 2.18.x

# Pin numpy to prevent upgrades
numpy==1.26.2

# Pin ML frameworks to exact versions
tensorflow==2.15.0
torch==2.1.2
transformers==4.36.0
spacy==3.7.2

# Pin data science packages
pandas==2.1.4
scikit-learn==1.3.2

# Prevent keras upgrade (tensorflow dependency)
keras>=2.15.0,<2.16.0

# Prevent protobuf issues
protobuf>=3.20.3,<5.0.0

# Prevent h5py issues
h5py>=3.10.0,<3.12.0
